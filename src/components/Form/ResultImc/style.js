import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerResultImc: {
    alignItems: 'center',
    },
  resultImc: {
    fontSize: 28,
    color: '#ff0043',
    fontWeight: 'bold',
  }
})

export default styles